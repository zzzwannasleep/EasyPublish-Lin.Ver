# 剧集项目 / 合集电影双流程改造方案

## 1. 背景

当前系统虽然已经引入了 `projectMode='episode' | 'feature'`，但现有实现本质上仍然是：

- 一个项目对应一份 `config.json`
- 一份 `config.json` 对应一个标题
- 一个标题对应一个种子
- 一个种子对应一组发布结果

这套结构适合：

- 合集
- 剧场版
- 电影
- 一次性准备完整发布稿的项目

但它不适合下面这种更常见的连载工作流：

- 一个项目代表一部剧
- 这部剧下面有很多集
- 每一集又有多个版本
- 每个版本可能有不同的清晰度、字幕、目标站点和发布进度

例如：

- `Project`: 《某动画 第二季》
- `Episode`: `01`
- `Variant A`: `1080p / 简中`
- `Variant B`: `2160p / 双语`

如果继续把“单集模式”理解成“一个项目就是一集”，后续会持续遇到这些问题：

1. 同一部剧无法集中管理
2. 同一集多个版本会互相覆盖
3. 发布进度只能按“当前这一条”看，不能按“整部剧”看
4. 很难做“继承上一集”“补发缺失版本”“按剧分组”这类高频需求

因此，这条线不应该只停留在“给单集加个表单”，而应该把模型改成真正的“剧项目工作台”。

## 2. 结论

核心结论只有两条：

1. `feature` 模式继续保留“一个项目 = 一个可发布成品”的现有模型
2. `episode` 模式应升级为“一个项目 = 一部剧”，真正可发布的最小单位改为“某一集的某个版本”

也就是说，用户看到的模型应该是：

- `剧项目`
- `集`
- `版本`

而不是：

- `单集项目`

为了控制改造成本，建议短期内保留内部字段值 `projectMode='episode'`，但用户可见文案从“单集模式”逐步改成“剧集模式”或“连载模式”。

## 3. 设计原则

### 3.1 先区分“容器”和“可发布单元”

在剧集模式下：

- `Project` 是容器
- `Variant` 才是可发布单元

这两个角色不能再混在一个结构里。

### 3.2 先做层级拆分，再做效率功能

如果底层还是“一项目一配置一发布结果”，那么下面这些功能都会越做越乱：

- 继承上一集
- 一键复制版本规格
- 同剧按集管理
- 批量补发
- 统计哪些版本还没发

所以必须先拆模型，再做增强功能。

### 3.3 默认值放上层，执行状态放下层

建议字段归属如下：

- `Project`：剧名、季度、默认发布站点、默认分类、默认发布规格、标题模板、简介模板
- `Episode`：集号、分集标题、分集备注、播出日期
- `Variant`：种子、标题、清晰度、字幕语言、编码、目标站点覆盖、发布阶段、发布结果

不要把所有字段都堆在同一张表单里。

### 3.4 防漏发必须以 `Variant.targetSites` 为准

发布结果只能说明“已经发了什么”，不能说明“本来应该发什么”。

因此：

- 计划发布站点必须持久化
- 应该挂在 `Variant` 上，而不是只挂在剧项目上

原因很简单：

- 同一部剧的 `1080p / 简中` 和 `2160p / 双语` 目标站点可能不同
- 同一集不同版本的补发进度也可能不同

### 3.5 尽量复用现有发布器，不强行重写底层能力

当前发布流程、内容生成、站点适配，本质上都更接近“发布某个版本”。

因此最稳的方案不是推翻它们，而是：

- 在上层新增剧项目和集的管理层
- 在下层继续复用现有“单条发布配置”的执行链路

换句话说，现有 `episode` 编辑器更像未来的“版本编辑器”，而不是“剧项目编辑器”。

## 4. 目标产品结构

### 4.1 创建入口

新建项目第一步只做模式选择：

- `剧集模式`
- `合集 / 电影模式`

选择后分流：

- `剧集模式` -> 创建剧项目壳
- `合集 / 电影模式` -> 继续走现有 `quick / file / template`

这里不应该再让用户在第一步面对：

- `quick`
- `file`
- `template`

因为这三个概念描述的是“生成方式”，不是“我要发布什么”。

### 4.2 剧集模式总流程

推荐结构：

1. 创建剧项目
2. 进入剧项目主页
3. 新增或选择某一集
4. 在该集下新增一个或多个版本
5. 打开某个版本进入编辑 / 生成 / 发布
6. 在剧项目主页查看整体进度

### 4.3 合集 / 电影模式总流程

继续保留当前结构：

1. 创建项目
2. 编辑项目
3. 检查输出
4. 发布种子
5. 发布主站
6. 完成结果

## 5. 数据模型建议

### 5.1 命名建议

用户视角：

- `Project` = 项目
- `Episode` = 集
- `Variant` = 版本

实现视角建议逐步演进为：

- `Project`: 顶层项目
- `PublishUnit`: 真正可发布单元

其中：

- `feature` 项目有且只有一个 `PublishUnit`
- `episode` 项目有很多 `PublishUnit`，每个 `PublishUnit` 对应“某一集的某一版本”

如果短期不想一次性引入 `PublishUnit` 命名，也至少要在文档和代码层明确：

- 当前 `PublishProject` 更像“发布单元摘要”
- 未来不能继续把它当成“剧项目”本身

### 5.2 顶层项目

```ts
type ProjectMode = 'episode' | 'feature'

interface ProjectSummary {
  id: number
  name: string
  projectMode: ProjectMode
  workingDirectory: string
  status: 'draft' | 'publishing' | 'published'
  createdAt: string
  updatedAt: string
}
```

剧集项目建议扩展为：

```ts
interface SeriesProject extends ProjectSummary {
  projectMode: 'episode'
  series: {
    titleCN?: string
    titleEN: string
    titleJP?: string
    seasonLabel?: string
    releaseTeam?: string
  }
  defaults: {
    targetSites: SiteId[]
    categoryBangumi?: string
    categoryNyaa?: string
    sourceType?: string
    videoCodec?: string
    audioCodec?: string
    summaryTemplate?: string
    variantPresets: VariantPreset[]
  }
  stats: {
    episodeCount: number
    variantCount: number
    pendingVariantCount: number
    publishedVariantCount: number
  }
}
```

合集 / 电影项目可以继续接近当前模型：

```ts
interface FeatureProject extends ProjectSummary {
  projectMode: 'feature'
  sourceKind: 'quick' | 'file' | 'template'
}
```

### 5.3 集模型

```ts
interface ProjectEpisode {
  id: number
  projectId: number
  episodeLabel: string
  episodeTitle?: string
  sortIndex: number
  airDate?: string
  note?: string
  stats: {
    variantCount: number
    pendingVariantCount: number
    publishedVariantCount: number
  }
  createdAt: string
  updatedAt: string
}
```

字段归属说明：

- `episodeLabel`：`01`、`02`、`SP1`、`01-02`
- `episodeTitle`：分集标题
- `sortIndex`：用于稳定排序，不依赖字符串比较

### 5.4 版本模型

```ts
interface EpisodeVariant {
  id: number
  projectId: number
  episodeId: number
  name: string
  path: string
  title: string
  stage: 'edit' | 'torrent_publish' | 'completed'
  status: 'draft' | 'publishing' | 'published'
  targetSites: SiteId[]
  publishResults: PublishResult[]
  spec: {
    sourceType: string
    resolution: string
    videoCodec: string
    audioCodec: string
    subtitleProfile: string
    variantLabel?: string
  }
  torrentPath: string
  createdAt: string
  updatedAt: string
}
```

这里有两个关键点：

1. `resolution` 和 `subtitleProfile` 必须拆字段，不能只拼在标题里
2. `targetSites` 必须在版本层可覆盖

字幕建议不要只用一个自由字符串，但也不要一开始就过度枚举死：

```ts
type SubtitleProfile =
  | 'chs'
  | 'cht'
  | 'eng'
  | 'chs-eng'
  | 'cht-eng'
  | 'bilingual'
  | 'custom'
```

如果用户的圈内命名是：

- `简`
- `繁`
- `英`
- `双语`

完全可以在 UI 上用这些文案，底层再映射到稳定值。

### 5.5 版本预设

为了支持“同一部剧高频重复发不同版本”，建议增加版本预设：

```ts
interface VariantPreset {
  id: string
  name: string
  sourceType: string
  resolution: string
  videoCodec: string
  audioCodec: string
  subtitleProfile: string
  targetSites: SiteId[]
}
```

例如：

- `1080p / 简中`
- `2160p / 双语`

新增一集时可以直接从预设生成多个版本草稿，而不是每次重复填写。

### 5.6 当前 `config.json` 的定位

当前 `Config.PublishConfig` 仍然有价值，但未来它应该只代表“一个版本的发布配置”，而不是整个剧项目。

因此建议：

- `feature` 项目：项目根目录直接保留当前 `config.json`
- `episode` 项目：每个版本目录各自拥有一份 `config.json`

也就是说，在剧集模式下：

- `project.json` 描述剧项目
- `episode.json` 描述某一集
- `config.json` 描述某个版本

### 5.7 建议的目录结构

```text
Series Project Root/
  project.json
  episodes/
    E01/
      episode.json
      variants/
        1080p-chs/
          config.json
          bangumi.html
          nyaa.md
          acgrip.bbcode
          xxx.torrent
        2160p-bilingual/
          config.json
          bangumi.html
          nyaa.md
          acgrip.bbcode
          xxx.torrent
    E02/
      episode.json
      variants/
        ...
```

这个结构的优点：

1. 版本目录可以继续复用现有生成和发布逻辑
2. 人工查看文件时也容易理解
3. 后续做导入、复制、批量操作都更自然

## 6. 字段归属建议

为了避免未来表单爆炸，建议从一开始就把字段分清：

### 6.1 放在剧项目上的字段

- 剧名中英日
- 季 / 篇章名称
- 默认发布组
- 默认 Bangumi / Nyaa 分类
- 默认目标站点
- 默认简介模板
- 默认版本预设

### 6.2 放在集上的字段

- 集号
- 分集标题
- 分集备注
- 播出日期

### 6.3 放在版本上的字段

- 清晰度
- 字幕语言
- 视频编码
- 音频编码
- 源类型
- 种子文件
- 发布标题
- 版本简介补充
- 版本目标站点
- 发布结果

一句话概括：

- “这一整部剧共享的东西”放项目上
- “这一集共享的东西”放集上
- “这一版独有的东西”放版本上

## 7. UI 与交互建议

### 7.1 新建项目页

第一步：

- `剧集模式`
- `合集 / 电影模式`

第二步分流：

- 剧集模式：只创建剧项目壳，录入剧信息和默认预设
- 合集 / 电影模式：继续现有 `quick / file / template`

### 7.2 剧项目主页

剧项目主页应该成为剧集模式的主工作台，而不是一上来就进入当前的版本编辑表单。

建议包含：

- 剧信息卡片
- 版本预设管理
- 集列表
- 最近待发布版本
- 发布统计
- “新增一集”
- “从上一集复制”

### 7.3 集详情页

进入某一集后，重点应该是“版本列表”，而不是直接开始填发布内容。

建议包含：

- 当前集基础信息
- 版本列表
- 每个版本的发布状态
- 每个版本的目标站点完成度
- “新增版本”
- “从预设生成版本”

### 7.4 版本编辑器

当前 `EpisodeEdit.vue` 建议逐步下沉为“版本编辑器”：

- 录入标题
- 录入种子
- 调整版本规格
- 选择目标站点
- 生成 `html / md / bbcode`
- 进入 BT 发布页

也就是说，它未来更适合改名为：

- `EpisodeVariantEdit.vue`

而不应该承担整个剧项目的管理职责。

### 7.5 发布页

发布页仍然围绕“当前版本”工作，但需要补两个层次的提示：

1. 当前版本还差哪些站点没发
2. 当前剧项目还有哪些集 / 版本没完成

第 1 层是主视角，第 2 层是辅助视角。

### 7.6 完成页

完成页不能再默认表达“整部剧都完成”，它只能表达：

- 这个版本完成了什么
- 还差哪些目标站点

如果需要展示整部剧进度，应该回到剧项目主页看。

### 7.7 项目列表

项目列表在剧集模式下应展示聚合信息，而不是只展示一个版本的链接：

- 剧项目名
- 集数
- 版本数
- 未完成版本数
- 最近更新
- 快速进入最近未完成版本

## 8. 与当前代码的对应关系

下面这部分是为了让后续实现不走偏。

### 8.1 当前实现的真实约束

当前这些结构本质上都还是“单条发布配置”：

- `src/shared/types/project.ts` 的 `PublishProject`
- `src/config.d.ts` 的 `Content_episode`
- `src/main/services/project-service.ts` 创建的 `config.json`
- `src/renderer/src/components/EpisodeEdit.vue`
- `src/main/services/content-service.ts` 的 `createWithEpisode`

它们都只适合代表“一个可发布版本”。

因此，不建议继续扩展它们去兼容“剧项目 + 多集 + 多版本”的所有信息。

### 8.2 推荐的重构边界

建议把当前模型拆成两层：

1. 顶层 `Project`
2. 发布单元 `Variant`

过渡期可以这样理解：

- 当前 `feature` 项目的 `Project` 和 `Variant` 是 1:1
- 当前 `episode` 项目在未来会变成：
  - 一个剧项目
  - 一个集
  - 一个版本

### 8.3 旧 `episode` 数据的迁移映射

如果历史上已经创建了一批 `episode` 项目，迁移规则建议如下：

1. 创建一个新的剧项目
2. 从原 `config.json` 读取剧名、季信息
3. 自动创建一个 `Episode`
4. 自动创建一个 `Variant`
5. 原 `publishResults` 全部挂到该 `Variant`

这样历史数据不会丢，但新的抽象层也能建立起来。

如果短期内不做自动迁移，也至少要允许：

- 旧 `episode` 项目继续按旧方式打开
- 新建的剧集模式项目走新结构

## 9. 推荐的实施顺序

### Phase 1：先改术语和设计边界

目标：

- 用户视角从“单集模式”改成“剧集模式”
- 团队内部统一认知：真正可发布的是版本，不是剧项目

任务：

1. 更新文档
2. 更新创建页文案
3. 不急着改底层执行链

验收：

- 后续开发不再把 `episode` 误解为“一项目一集”

### Phase 2：建立剧项目 / 集 / 版本三层结构

目标：

- 新增剧项目壳
- 新增集与版本的存储结构

任务：

1. 新增顶层项目元数据
2. 新增集记录
3. 新增版本记录
4. 建立版本目录结构

验收：

- 一个剧项目下可以创建多集
- 一集下可以创建多个版本

### Phase 3：把当前单集编辑器下沉为版本编辑器

目标：

- 复用现有发布链

任务：

1. `EpisodeEdit.vue` 改造成版本编辑器
2. `Content_episode` 只服务版本级配置
3. `createWithEpisode` 只对版本目录生效

验收：

- 任意一个版本都能独立生成内容并进入发布页

### Phase 4：聚合展示与防漏发

目标：

- 让剧项目工作台真正有管理价值

任务：

1. 剧项目主页显示集 / 版本统计
2. 项目列表显示未完成版本数
3. 发布页显示当前版本剩余目标站点
4. 完成页禁止误导用户“整剧已完成”

验收：

- 一眼看出哪个剧、哪一集、哪个版本还没发完

### Phase 5：效率增强

目标：

- 让剧集模式真正适合高频日常使用

任务：

1. 从上一集复制
2. 按版本预设批量生成
3. 批量创建下一集
4. 批量补发缺失站点

## 10. 验收标准

### 功能验收

1. 新建剧集模式项目后，得到的是“剧项目工作台”，不是直接的一条单集发布记录
2. 一个剧项目下可以有多集
3. 一集下可以有多个版本
4. 每个版本有独立的标题、种子、目标站点、发布结果
5. 一个版本的发布失败不会污染其他版本状态
6. 合集 / 电影模式保持现有能力不回退

### 交互验收

1. 用户创建项目时先按“我要发剧还是发电影”来思考
2. 用户在剧项目页可以看到整体进度
3. 用户在集页可以快速新增多个版本
4. 用户在版本页可以继续沿用熟悉的发布流程

### 技术验收

1. 现有内容生成和站点发布逻辑尽量复用
2. 历史 `feature` 数据不需要破坏性迁移
3. 历史 `episode` 数据有兼容策略
4. 文件目录结构能支撑后续批量操作

## 11. 明确不建议的方案

### 11.1 不建议继续把“单集项目”往上堆功能

如果继续沿用当前“一项目一配置”的单集模型，再往上叠：

- 多集
- 多版本
- 聚合进度
- 继承复制

最终只会得到一个越来越难维护的巨型对象。

### 11.2 不建议把多个版本塞进同一份 `config.json`

这样会导致：

1. 发布状态不独立
2. 文件产物互相覆盖
3. 发布器无法复用

### 11.3 不建议让项目级 `targetSites` 直接代表最终发布计划

项目级默认值可以有，但最终完成度必须按版本计算。

否则：

- `1080p` 发四站
- `2160p` 只发一站

这种常见场景就会失真。

## 12. 推荐的后续任务单

可以按下面的任务拆分：

1. `docs(workflow): redefine episode mode as series project with episode/variant hierarchy`
2. `feat(create): rename episode mode to series mode in user-facing copy`
3. `feat(project): add series project metadata and dashboard shell`
4. `feat(episode): add episode list and per-episode variant management`
5. `refactor(editor): turn EpisodeEdit into a variant editor`
6. `refactor(content): scope Content_episode and createWithEpisode to variant config`
7. `feat(progress): aggregate pending variants and missing target sites at project level`

---

这份方案的重点不是“把单集表单再优化一点”，而是明确新的长期方向：

- `feature` 继续是一条成品发布流
- `episode` 升级为剧项目工作台
- 真正可发布的对象是“某一集的某一版本”
## 13. 当前落地进度（2026-03-15）

- 剧集模式已经从“直接打开单集表单”改成“先进入剧集工作台”
- 工作台已支持三层结构：项目 / 集 / 版本
- 版本已经支持结构化预设字段：`videoProfile` / `subtitleProfile`
- 当前已支持的预设包括：`1080p / 1K`、`2160p / 4K`、`简中`、`繁中`、`英文`、`双语`
- 现在既可以单个创建版本，也可以按预设矩阵批量生成版本
- 当前集已经支持“继承上一集版本矩阵”
- 激活某个版本后，现有编辑器仍复用根目录草稿，但保存时会自动同步回该激活版本

### 13.1 仍然是过渡态的部分

- `EpisodeEdit` 还没有彻底下沉成真正的版本级编辑器
- 版本预设目前仍写死在工作台逻辑里，还不是项目级可配置模板
- 版本级默认站点、标题模板、简介模板还没有完全独立出来

### 13.2 下一步建议

1. 把版本预设从固定枚举提升成项目级模板
2. 让批量生成支持“保存为常用矩阵”
3. 把版本级默认站点和标题/简介模板下沉到版本配置层

### 13.3 新增进度补充

- 当前已经支持把批量预设矩阵保存为项目级模板
- 模板支持在工作台里直接套用和删除

### 13.4 新增进度补充

- 项目级版本模板现在支持记录默认目标站点
- 项目级版本模板现在支持记录标题模板
- 批量生成版本时，会把模板里的默认目标站点写入对应版本 `config.json`
- 批量生成版本时，会按标题模板预填版本标题，后续仍可在编辑器里继续手动调整

### 13.5 新增进度补充

- 项目级版本模板现在支持记录简介模板
- 批量生成版本时，会把简介模板写入对应版本的 `content.summary`
- 简介模板和标题模板共用同一套变量占位符，可直接引用当前草稿里的标题、简介和剧集字段

### 13.6 新增进度补充

- 单个版本创建现在也可以显式套用当前模板默认值
- 单个版本创建启用默认值后，会和批量生成版本一样写入目标站点、标题模板和简介模板
- 工作台会在单个版本创建区直接预览当前会套用的默认站点、标题预设和简介预设

## 14. 基于使用反馈的下一轮交互重构方向（2026-03-15）

### 14.1 这次反馈的核心，不是“再补几个字段”

当前剧集工作台虽然已经支持：

- 版本矩阵
- 项目级模板
- 默认目标站点
- 标题模板
- 简介模板

但它的操作心智仍然更像：

1. 先创建集
2. 先创建版本
3. 再进入当前草稿逐步补标题、站点和发布参数

而更符合日常连载使用习惯的心智应该是：

1. 先定义“这部剧平时怎么发”
2. 把这套规则保存成可复用的发布配置
3. 发布下一集时直接选择该项目和该配置
4. 只补本集差异，然后直接发布

也就是说，下一轮优化的重点应该从“版本生成器”转向“剧项目发布配置中心”。

### 14.2 建议新增一个明确对象：`剧项目发布配置`

建议把当前偏技术化的“版本模板 / 矩阵模板”进一步提升为用户更容易理解的对象：

- `发布配置`
- 或 `剧集发布方案`

这个对象建议挂在剧项目下面，而不是挂在某一集下面。

它负责描述的不是“某一集已经发了什么”，而是“这部剧以后每一集大概率都会怎么发”。

一个发布配置至少应包含：

- 配置名称，例如 `1080p 简中常规版`、`2160p 双语完整版`
- 标题预设
- 发布稿模板
- 默认发布站点
- 各站点默认字段
- 版本规格字段，例如分辨率、字幕、制作组、编码信息

当前已有的 `variantTemplates` 可以视为这个对象的前身，但它们现在还偏重“批量生成组合”，还没有真正覆盖“站点发布配置复用”。

### 14.3 推荐的编辑顺序

当用户在剧集模式下编辑项目时，推荐把界面主顺序调整为：

1. `标题预设`
2. `发布稿模板`
3. `发布站点选择`
4. `已选站点的必填 / 选填项`
5. `保存为发布配置`

这条顺序更贴近真实操作习惯：

- 先决定这一类版本的标题长什么样
- 再决定各站点的稿件怎么生成
- 再决定这类版本默认发哪些站
- 最后只处理已选站点真正需要填写的参数

### 14.4 标题预设应该成为第一块主配置

标题预设不应该只是一个普通文本框，而应该是一套可组合的字段序列。

建议标题预设支持：

- 固定文本片段
- 内置变量片段
- 自定义输入字段
- 实时预览最终标题

内置变量建议至少覆盖：

- `集数`
- `分辨率`
- `字幕`
- `制作组`
- `片名`
- `季度`
- `音视频规格`

例如一个常见标题预设可以是：

`[制作组] 片名 - 集数 [分辨率][字幕]`

关键点不是把所有内容都写死，而是让用户能按自己的习惯调整字段顺序、增删字段和补固定文案。

### 14.5 发布稿模板应该按站点维度组织

标题预设下面，建议直接进入“各站点发布稿模板”区域。

这里建议按站点卡片或站点分组展示，每个站点都可以定义：

- 该站点用什么稿件格式
- 该站点的标题是否沿用全局标题，还是单独覆盖
- 该站点的简介 / BBCode / Markdown 模板
- 该站点需要的默认标签、分类、附加说明

这样做的意义是：

- 标题解决“名字怎么拼”
- 发布稿模板解决“正文怎么写”
- 站点字段解决“这个站具体还缺什么”

三层职责清晰，不会把所有逻辑堆进同一块表单。

### 14.6 站点选择之后，只展开真正需要填写的站点字段

发布站点选择建议放在发布稿模板之后，但站点字段区只为“已选择且当前账号具备发布权限”的站点展开。

具体规则建议明确为：

- 未选择的站点，不显示详细字段
- 已选择但当前没有账号或没有发布权限的站点，显示禁用状态和原因
- 已选择且可发布的站点，才展开必填项和选填项

站点字段的展示应尽量走站点能力声明，而不是继续在 UI 里手写站点名分支。

也就是说，站点适配器应该告诉前端：

- 这个站点有哪些必填项
- 哪些是选填项
- 哪些字段有默认值
- 哪些字段可以从项目级配置继承

这样界面才能随着站点能力扩展，而不是每加一个站点就继续堆条件分支。

### 14.7 “下一集复用”应成为剧集模式的核心价值

用户真正想要的结果不是“更复杂的当前集表单”，而是：

1. 先建立一个剧项目
2. 在项目里保存一到多个常用发布配置
3. 下一次发布新一集时，直接进入该项目
4. 选择要套用的发布配置
5. 自动带出标题规则、发布稿模板、目标站点和站点默认字段
6. 只修改本集差异后直接发布

这里的“本集差异”通常只包括：

- 当前集号
- 当前分集标题
- 本集种子 / 文件
- 某些站点临时需要改单独字段

换句话说，剧项目应该保存“长期稳定的配置”，单集只负责填写“本次变化的值”。

### 14.8 和当前实现的衔接方式

为了控制改造成本，建议不要推翻现有工作台，而是在当前实现基础上迭代：

1. 短期把当前“模板”文案和结构逐步升级为“发布配置”
2. 先把标题预设、简介模板、默认站点、站点默认字段收敛到同一个配置对象
3. 单版本创建和批量创建都支持“从发布配置创建”
4. 下一集创建时优先提供“选择已有发布配置”入口，而不是先从空白版本开始

这样可以继续复用：

- 当前剧项目 / 集 / 版本三层结构
- 当前版本激活与草稿同步机制
- 当前目标站点写入版本 `config.json` 的链路
- 当前标题模板 / 简介模板变量系统

真正要改变的是“入口和交互顺序”，而不是把底层发布器整套重写。

### 14.9 这一轮优化后的理想用户路径

理想路径应该收敛成下面这样：

1. 新建 `剧项目`
2. 在项目里配置一个或多个 `发布配置`
3. 每个发布配置定义：
   - 标题预设
   - 发布稿模板
   - 默认站点
   - 各站点默认字段
4. 新一集到来时，只需要：
   - 选择剧项目
   - 选择集
   - 选择一个发布配置
   - 导入本集资源
   - 检查后发布

如果后续要继续抽象命名，当前的 `SeriesVariantTemplate` 最终更适合演进成：

- `SeriesPublishProfile`
- 或 `SeriesReleaseConfig`

这样命名会比“模板”更贴近用户实际理解。

## 15. 可直接落地的页面结构草案

### 15.1 剧项目主页应拆成两个主区域

当前工作台更偏“集 / 版本管理台”。

下一轮建议明确拆成：

1. `发布配置区`
2. `集与发布记录区`

也就是说，剧项目主页首先回答的是：

- 这部剧平时有哪些发布方案
- 下一集准备套哪套方案

而不是一上来就让用户先面对版本矩阵。

### 15.2 发布配置区建议的版块顺序

发布配置区建议固定为以下顺序：

1. `配置列表`
2. `基础信息`
3. `标题预设`
4. `站点稿模板`
5. `默认发布站点`
6. `站点默认字段`
7. `保存 / 另存为 / 复制`

各块职责建议如下：

- `配置列表`
  - 展示当前剧项目下已有的发布配置
  - 支持新建、复制、删除、设为默认
- `基础信息`
  - 配置名称
  - 默认版本名称
  - 默认分辨率 / 字幕 / 制作组 / 编码字段
- `标题预设`
  - 拼装标题字段序列
  - 实时预览
- `站点稿模板`
  - 按站点配置正文模板和站点专属标题覆盖
- `默认发布站点`
  - 只勾选计划发布的站点
- `站点默认字段`
  - 只对已勾选站点展示字段
  - 并标明必填 / 选填 / 未授权

### 15.3 集与发布记录区建议的版块顺序

配置区下面再进入执行层：

1. `集列表`
2. `当前集已套用配置`
3. `本集差异字段`
4. `本集版本列表`
5. `进入编辑 / 检查 / 发布`

这里建议弱化“先创建版本矩阵”的存在感，强化“本集套用哪套配置”的操作入口。

更符合使用习惯的顺序应该是：

1. 选中第 `01` 集
2. 点击 `套用 1080p 简中常规版`
3. 系统自动生成对应版本和站点默认值
4. 用户只补本集差异
5. 进入原有编辑器或直接发站

### 15.4 新建下一集时的推荐弹层

建议在“新增一集”之后，直接弹出一个轻量流程，而不是让用户自己再去手动点很多次：

1. 创建集成功
2. 立即弹出“选择发布配置”
3. 可选：
   - `套用默认配置`
   - `从上一集继承`
   - `从空白开始`
4. 选择后自动生成当前集的初始版本

这样用户发布下一集时，不需要再先理解“模板”、“矩阵”、“默认值套用开关”等内部概念。

### 15.5 当前版本卡片建议展示的信息

如果仍保留版本列表，建议每个版本卡片重点展示：

- 版本名称
- 套用的发布配置名称
- 标题预览
- 目标站点数量
- 已发布 / 待发布 / 失败站点数
- 最近更新时间

而不是只展示目录和更新时间。

因为用户在剧集模式里最关心的是：

- 这一版到底是什么
- 这一版还差哪些站
- 这一版是否沿用了正确配置

## 16. `发布配置` 数据结构建议

### 16.1 推荐字段

建议在剧项目下新增一个稳定对象：

```ts
interface SeriesPublishProfile {
  id: number
  projectId: number
  name: string
  isDefault?: boolean
  variantDefaults: {
    variantName?: string
    resolution?: string
    subtitleProfile?: string
    releaseTeam?: string
    sourceType?: string
    videoCodec?: string
    audioCodec?: string
  }
  titlePreset: {
    template: string
    tokens: string[]
  }
  siteDrafts: Record<
    SiteId,
    {
      enabled: boolean
      titleTemplate?: string
      summaryTemplate?: string
      fieldDefaults?: Record<string, unknown>
    }
  >
  createdAt: string
  updatedAt: string
}
```

这里最关键的是两点：

1. `siteDrafts` 既表示“是否发这个站”，也表示“这个站默认怎么发”
2. 标题预设、站点稿模板、站点字段默认值必须在同一个对象里，不能继续散落在多个临时表单状态里

### 16.2 和当前 `SeriesVariantTemplate` 的关系

建议关系这样处理：

- 当前 `SeriesVariantTemplate` 不直接删除
- 先把它视为 `SeriesPublishProfile` 的简化前身
- 第一阶段通过迁移脚本或运行时兼容，把旧模板映射成新配置对象

映射关系建议如下：

- `videoProfiles / subtitleProfiles`
  - 继续作为默认版本规格来源
- `targetSites`
  - 映射到 `siteDrafts[siteId].enabled`
- `titleTemplate`
  - 映射到 `titlePreset.template`
- `summaryTemplate`
  - 映射到每个已启用站点的默认正文模板，或先挂到通用默认模板字段上

也就是说，旧模板不是废掉，而是升级。

### 16.3 配置与版本的关系

建议版本实例里新增以下关联：

```ts
interface EpisodeVariant {
  // existing fields...
  publishProfileId?: number
  publishProfileSnapshot?: SeriesPublishProfileSnapshot
}
```

其中：

- `publishProfileId` 用来说明这个版本最初套用了哪套配置
- `publishProfileSnapshot` 用来冻结创建当时的配置快照

必须保留快照，而不能只靠 `publishProfileId` 回查当前配置。

否则一旦项目级配置后续被修改：

- 历史版本标题会漂移
- 历史站点字段会漂移
- 无法追溯当时到底按什么规则发的

### 16.4 为什么要有快照

剧集模式天然是“长期项目”。

同一部剧从第 01 集发到第 12 集，中间经常会出现：

- 制作组名调整
- 标题写法调整
- 某个站点分类调整
- 某个站点停发或新增

如果版本只引用项目级“最新配置”，历史记录一定会失真。

所以建议规则明确为：

- `发布配置` 负责复用
- `版本快照` 负责留档

## 17. 建议的开发顺序

### 17.1 Phase A：先改名，再改入口

先做低风险重构：

1. 用户可见文案把“模板”逐步改成“发布配置”
2. 单版本创建区把“套用模板默认值”改成“选择发布配置”
3. 集创建完成后增加“立即选择配置”入口

这一阶段的目标不是做完所有能力，而是先把用户心智拉正。

### 17.2 Phase B：把配置对象收口

第二阶段再做数据层收口：

1. 新增 `SeriesPublishProfile`
2. 把标题模板、简介模板、默认站点整合进该对象
3. 为版本写入 `publishProfileId` 和配置快照
4. 兼容读取旧 `SeriesVariantTemplate`

这一阶段完成后，配置才真正成为稳定实体。

### 17.3 Phase C：按站点能力动态渲染字段

第三阶段再处理站点差异：

1. 站点适配器输出字段 schema
2. 配置编辑页按 schema 渲染必填 / 选填项
3. 未授权站点显示禁用原因
4. 版本创建时把站点默认字段带入站点草稿

这一阶段完成后，发布配置才真正覆盖“各站点怎么发”。

### 17.4 Phase D：优化下一集高频操作

最后再做效率优化：

1. 新增“创建下一集并套用默认配置”
2. 新增“从上一集复制本集差异外的全部配置”
3. 新增“按配置补发缺失站点”
4. 在项目首页突出显示“下一步建议动作”

这部分是体验增强，但应该放在前面三层结构稳定之后。

## 18. 对当前实现的直接改造建议

如果要从现有代码直接开始切，建议优先动下面几处：

1. [SeriesProjectWorkspace.vue](/c:/Users/65282/Downloads/PT自动发布脚本/src/renderer/src/features/project-editor/SeriesProjectWorkspace.vue)
   - 先把“批量预设 / 已保存模板”语义调整为“发布配置”
2. [workflow.ts](/c:/Users/65282/Downloads/PT自动发布脚本/src/renderer/src/locales/messages/zh-CN/workflow.ts)
   - 重写剧集工作台相关文案，弱化“矩阵”，强化“配置复用”
3. [workflow.ts](/c:/Users/65282/Downloads/PT自动发布脚本/src/renderer/src/locales/messages/en-US/workflow.ts)
   - 同步英文文案
4. 项目类型与桥接层
   - 把当前模板对象逐步扩展成稳定的发布配置对象

如果只做第一轮最小改动，甚至可以先不改底层存储结构，只先改：

- 文案
- 版块顺序
- “新增一集后立即套用配置”的入口

这样能先验证这条交互方向是不是更顺手。

## 19. 配套规格文档

为了避免这份文档继续同时承担：

- 产品方向
- 数据模型
- UI 结构
- 视觉细节

建议后续把页面结构、状态规则、样式约束单独维护在：

- [docs/series-publish-profile-ui-spec.md](/c:/Users/65282/Downloads/PT自动发布脚本/docs/series-publish-profile-ui-spec.md)
- [docs/series-publish-profile-wireframes.md](/c:/Users/65282/Downloads/PT自动发布脚本/docs/series-publish-profile-wireframes.md)

分工建议明确为：

- 本文档：负责“为什么这样设计、对象怎么建模、开发顺序怎么拆”
- UI 规格文档：负责“页面怎么摆、状态怎么显、样式怎么约束、交互怎么落”
- 低保真草图文档：负责“每个区域在屏幕上大概长什么样、区块先后顺序是什么”
